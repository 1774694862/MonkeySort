def test(a: int, b: int, c: int, d: int, e: int, f: int, g: int, h: int) -> int:
    if a == 1:
        return a + b + c + d + e + f + g + h
    else:
        return a + b + c + d + e + f + g + h + 1
